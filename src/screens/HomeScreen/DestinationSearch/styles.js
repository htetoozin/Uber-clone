import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 10,
    backgroundColor: '#fff',
    height: '100%',
  },
  textInput: {
    height: 50,
    padding: 10,
    marginVertical: 5,
    backgroundColor: '#eee',
  },
});

export default styles;
